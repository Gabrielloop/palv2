import React, {FC, useState} from 'react';
import LoginForm from "../components/auth/LoginForm";

// Comlposant de la page de connexion

const Login: FC<{}> = ({}) => {
    return (
        <>
           <LoginForm/>
        </>
    );
};

export default Login;
